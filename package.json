{
  "name": "crystal-vanguard",
  "version": "1.0.0",
  "description": "Crystal Vanguard - A premium fantasy arcade shooter built with Phaser 3",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.4.0",
    "gh-pages": "^6.1.0"
  }
}
